apiVersion: v1
kind: Service
metadata:
  name: {{ include "spscs.fullname" . }}
  labels:
    {{- include "spscs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spscs.selectorLabels" . | nindent 4 }}
